import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  boxForm: {
    display: "flex",
    flexWrap: "nowrap",
  },
  typographyForm: {
    padding: "8px",
  },
}));

export default useStyles;
